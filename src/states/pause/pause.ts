import { playSound } from '@/global/sounds'

export const playOpenSound = () => {
	playSound('JDSherbert - Ultimate UI SFX Pack - Popup Open - 1', { volume: -40 })
}
export const playCloseSound = () => {
	playSound('JDSherbert - Ultimate UI SFX Pack - Popup Close - 1', { volume: -40 })
}
