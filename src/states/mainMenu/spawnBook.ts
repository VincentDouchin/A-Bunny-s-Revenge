export const spawnBook = () => {
	// const mainMenuRenderGroup = mainMenuRenderGroupQuery.first
	// const camera = mainMenuCameraQuery.first
	// if (!mainMenuRenderGroup || !camera) return
	// // ecs.add({
	// // 	light: new AmbientLight(),
	// // 	position: new Vector3(),
	// // 	parent: mainMenuRenderGroup,
	// // })

	// ecs.add({
	// 	model: bookModel,
	// 	position: new Vector3(),
	// 	parent: mainMenuRenderGroup,
	// })
	// bookModel.traverse((node) => {
	// 	if (node instanceof Mesh && node.name === 'pageleft') {
	// 		node.material = drawnHouseShader
	// 	}
	// })
}